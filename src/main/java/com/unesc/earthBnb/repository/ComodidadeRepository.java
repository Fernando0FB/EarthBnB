package com.unesc.earthBnb.repository;

import com.unesc.earthBnb.model.Comodidades;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ComodidadeRepository extends JpaRepository<Comodidades, Long> {
}
